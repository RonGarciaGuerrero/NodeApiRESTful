<%- include("template/cabecera",{tituloWeb:'Registro de usuarios'})%>
<div class="container mb-4">
    <h1>Registrese en nuestra tienda</h1>
    <% if (error) { %>
        <span class="error"><%= error %></span>
        <% } %>   
        <form id="registro" action="/pokemon/register" method="POST">
            <input type="text" class="form-control my-2" name="login" id="login" placeholder="Indique nombre de usuario">
            <input type="password" class="form-control my-2" name="password" id="password" placeholder="Indique una contraseña">

            <button class="btn btn-warning btn-sm" type="submit">
                Crear usuario
            </button>
        </form>
 
</div>
<%- include("template/footer") %> 